<resources>
    <string name="app_name">Noticon</string>

    <!--
        模块描述，被 AndroidManifest 的 android:description 引用。

        这是 LibXposed（API 102）模块在管理器里显示描述的**唯一**字段：
        管理器读的是 ApplicationInfo.descriptionRes。
        老式写法（xposeddescription meta-data / 同名字符串资源）已经全部删掉，
        那是给 API <= 93 的旧框架用的，留着反而让模块变成"兼容模式"。

        长度约定：中文 <= 26 字，太长在模块列表里会占好几行。
        当前是 26 字。
    -->
    <string name="xposed_description">把未适配的通知小图标换成应用图标，保留原色；已适配的不动</string>
</resources>
