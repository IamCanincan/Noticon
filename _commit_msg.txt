feat: 恢复桌面图标并新增 MD3E 设置界面，提供两种图标模式

界面（Compose + Material 3 Expressive）：
- 新增 ui/MainActivity 与 ui/SettingsScreen，桌面上重新有入口
- 两种模式：彩色桌面图标（默认，保留原色）/ 系统黑白通知（压应用原始小图标成单色）
- 主题用 MaterialExpressiveTheme + Android 12+ 动态取色，低版本用樱粉兜底配色

配置通道：
- 新增 data/ModulePrefs：界面写 SharedPreferences("noticon")，
  模块侧在 SystemUI 进程用 LibXposed getRemotePreferences 读同一个文件
- 框架未声明 PROP_CAP_REMOTE 或界面从未打开时，退回原有默认值继续工作

其他：
- 模块描述更新为「把未适配的通知小图标换成应用图标或单色，已适配的不动」
- check-device.sh 不再 force-stop SystemUI（会把用户壁纸清成默认），改为重启设备
- 版本 1.1.0（versionCode 3），补 metadata changelog

构建注意：
- material3 用 1.5.0-alpha28：MD3E 公开 API 在 1.4.0 稳定版里仍是 internal
- 强制 compose-group-mapping 到 2.4.20：AGP 9.4.1 索要的 2.2.10 从未发布过，
  且只从 dl.google.com 解析，会让 release 构建直接失败
