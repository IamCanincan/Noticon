fix: 缩短模块描述到 26 字

Vector 里描述已经能显示了（靠 android:description），但 60+ 字的
长描述在模块列表里占太多行，用户反馈「太长了」。

定案（26 字）：
  把未适配的通知小图标换成应用图标，保留原色；已适配的不动

同步改 3 处（少一处就会显示不一致）：
  - res/values/strings.xml: xposed_description（android:description 引用）
  - res/values/strings.xml: xposeddescription（老框架反射读）
  - AndroidManifest.xml: xposeddescription meta-data（必须是字面量）

另外 META-INF/xposed/module.prop 的英文 description 也缩短，
并修正里面一句过时注释（描述真正来源是 android:description，
不是 xposeddescription 资源）。

验收：
  aapt2 dump xmltree → android:description=@0x7f0b0009 存在
  aapt2 dump resources → () "把未适配的通知小图标换成应用图标，保留原色；已适配的不动"
真机 install -r 成功。
